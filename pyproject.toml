[project]
name = "usecasemaif"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Antonin Rouxel", email = "antonin.rouxel44@outlook.fr" }
]
requires-python = ">=3.12"
dependencies = [
    "whisperx>=3.7.4",
]

[project.scripts]
usecasemaif = "usecasemaif:main"

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"
